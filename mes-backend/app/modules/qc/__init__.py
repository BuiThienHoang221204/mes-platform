"""Trạm 2 — QC. Vai QC.

    models.py  schemas.py  repository.py  service.py  router.py
    bảng: qc_result

FAIL trả MO về KHO (0) và mở vòng mới — nên module này phụ thuộc `round`.
Về Bàn team leader (3) thì MO nhảy qua Setup và QC, hàng lỗi đi thẳng vào chuyền (§5).
"""
