"""Danh mục chỉ đọc cho các ô chọn của FE.

    models.py  schemas.py  repository.py  router.py
    bảng: line · reason_code          (không có service.py)

Không có luật nghiệp vụ nào — dựng service rỗng cho đủ bộ là dựng tầng cho có.
Chỉ `production` import nó, để tra chuyền theo mã.
"""
